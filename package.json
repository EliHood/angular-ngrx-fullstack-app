{
  "name": "angexample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd server && npm run server",
    "client": "cd ./client && ng serve --proxy-config proxy.conf.json",
    "startdev": "concurrently --kill-others \"npm run client\" \"npm run server\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
